import styled from "styled-components";

export const LogoContainer = styled.div`
    display: flex;
    z-index: 3;
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    padding: 30px 40px;
`;