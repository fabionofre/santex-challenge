import styled from "styled-components";

export const ListContainer = styled.div`
    width: 90%;
    margin-top: 150px;
    display: flex;
    flex-wrap: wrap;
    gap: 20px;
    justify-content: center;
    z-index: 2;
`;