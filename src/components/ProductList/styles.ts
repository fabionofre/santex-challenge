import styled from "styled-components";

export const ListContainer = styled.div`
    margin-top: 150px;
    display: flex;
    flex-wrap: wrap;
    gap: 20px;
    justify-content: center;
    z-index: 2;
`;